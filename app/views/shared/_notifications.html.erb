<% unless flash.empty? %>
    <script>
        <% flash.each do |key, value| %>
            <% type = key.to_s.gsub('alert', 'error').gsub('notice', 'info')%>
            new Noty ({
                type: '<%= type %>',
                layout: 'topRight',
                timeout: '5000',
                theme: 'bootstrap-v3',
                text: '<%= value %>'
            }).show();
        <% end %>
   </script>
<% end %>